# configure eureka client
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka
    healthcheck:
      enabled: true
spring:
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
